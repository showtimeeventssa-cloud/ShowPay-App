# ShowPay — GitHub Upload Ready v16

Upload the CONTENTS of this folder directly into the root of the `main` branch.

Required root files include `index.html`, `app.js`, `styles.css`, and the `assets/` folder.
GitHub Pages should use: Deploy from a branch → `main` → `/(root)`.

No GitHub Actions workflow is included because branch-based Pages deployment does not require a custom workflow.
